# Toss_menu 협업 규칙

이 문서는 토스 공모전 앱을 함께 개발할 때 지켜야 하는 기본 협업 규칙입니다. 모든 팀원은 작업 전 이 문서를 먼저 확인합니다.

## 브랜치 규칙

- `main` 브랜치는 배포 가능한 안정 버전만 유지합니다.
- 개인 작업은 반드시 각자의 기능 브랜치에서 진행합니다.
- `main`에 직접 커밋하거나 직접 push하지 않습니다.
- 브랜치 이름은 아래 형식을 사용합니다.

```text
feature/member-seo
feature/member-park
feature/member-maeng
feature/member-oh
```

- 기능 단위로 더 나누어야 할 경우 아래 형식을 사용합니다.

```text
feature/member-oh/pet-home-drag
fix/member-seo/cart-reset
design/member-park/home-ui
```

## 작업 시작 전

작업 전 항상 원격 변경사항을 확인합니다.

```bash
git fetch origin
git status
```

현재 브랜치가 본인 작업 브랜치인지 확인합니다.

```bash
git branch --show-current
```

다른 사람이 수정한 파일을 덮어쓰지 않도록, 작업 전 변경 파일을 확인합니다.

```bash
git status --short
```

## 커밋 메시지 규칙

커밋 메시지는 반드시 Conventional Commits 형식을 따릅니다.

또한 커밋 메시지는 반드시 한글로 작성합니다.

형식:

```text
타입: 변경 내용 요약
```

사용 가능한 타입:

```text
feat: 새로운 기능 추가
fix: 버그 수정
design: UI, 스타일, 레이아웃 수정
refactor: 기능 변화 없는 코드 구조 개선
docs: 문서 수정
test: 테스트 추가 또는 수정
chore: 설정, 빌드, 패키지 등 기타 작업
```

좋은 예:

```text
feat: 펫홈 드래그 먹이기 기능 추가
fix: 구매 완료 후 장바구니가 비워지지 않는 문제 수정
design: 맞춤 추천 카드 모바일 간격 조정
docs: 윈도우 실행 방법 추가
refactor: 메뉴 선택 상태와 구매 상태 분리
```

나쁜 예:

```text
update
fix bug
feat: add pet drag
수정함
```

커밋은 가능한 한 하나의 목적만 담습니다. 여러 기능을 한 커밋에 섞지 않습니다.

## Pull Request 규칙

- `main`으로 직접 push하지 않고 Pull Request를 통해 병합합니다.
- PR 제목도 한글로 작성합니다.
- PR에는 아래 내용을 포함합니다.

```text
## 작업 내용
- 변경한 기능 또는 화면

## 확인 방법
- 실행한 명령어
- 직접 확인한 화면 또는 흐름

## 영향 범위
- 영향을 받는 화면, 파일, 기능
```

## 리뷰 규칙

- 리뷰어는 기능 동작, 사용자 불편, 모바일 화면 깨짐, 기존 기능 회귀를 우선 확인합니다.
- 작성자는 리뷰 의견을 반영한 뒤 다시 확인 요청을 남깁니다.
- 의견이 갈리면 사용자 편의성과 MVP 안정성을 우선합니다.

## 실행 및 검증

로컬 실행:

```bash
npm install
npm run dev
```

Windows PowerShell에서 `npm.ps1` 실행 정책 오류가 나면 아래 명령을 사용합니다.

```bash
npm.cmd run dev
```

커밋 또는 PR 전 최소 확인:

```bash
npm.cmd run build
npm.cmd run lint
```

## 코드 작성 원칙

- 기존 구조를 크게 바꾸기보다 현재 컴포넌트와 데이터 흐름을 우선 활용합니다.
- 사용자가 직접 보는 문구는 자연스러운 한글로 작성합니다.
- MVP에서는 기능이 복잡해지는 것보다 사용자가 이해하기 쉬운 흐름을 우선합니다.
- 모바일 화면에서 버튼, 카드, 문구가 겹치지 않도록 확인합니다.
- 실제 결제나 주문이 발생하지 않는 기능은 사용자에게 명확히 안내합니다.

## 충돌 처리

- 충돌이 나면 임의로 상대방 코드를 삭제하지 않습니다.
- 충돌 파일을 확인하고, 의도가 불명확하면 작성자에게 먼저 확인합니다.
- 해결 후 반드시 빌드와 린트를 다시 실행합니다.

## 금지 사항

- `main` 브랜치 직접 작업 금지
- 의미 없는 커밋 메시지 금지
- 영어 커밋 메시지 금지
- 확인하지 않은 대규모 덮어쓰기 금지
- 다른 팀원의 변경사항 임의 삭제 금지
